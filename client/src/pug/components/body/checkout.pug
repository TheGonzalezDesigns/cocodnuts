transition(name='fade' v-if=`view === 'checkout'`): #checkout
	//form#payment-form
		.form-row
			label(for="card-element")
				#card-element
				//input(for='item in hidden_data' type='hidden' :name='item.name' :value='item.value')
		#card-errors(role="alert")
		input.button.is-success(type="submit")
	#receipt: ul
		li.h1 Your Order
		li(v-for="item in order.list")
			.name {{item.name}}
			.total 
				| {{item.quantity}}
				| x
				| {{item.price}}
				| =
				| {{item.total | sign}}
		li.seperate
				.name Your total
				.total {{order.total | sign}}
		li.seperate
			input.button.is-success(type="submit")
	button.delete.is-large(@click='togglePane("menu")')
